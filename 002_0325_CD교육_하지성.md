# 📘 수업 노트 — 2026.03.25

---

## 1. 📁 PyQt5를 이용한 EXE 파일 제작
- 출퇴근 기록 프로그램 제작 기록 공유

---

## 2. 📰 뉴스 크롤링 프로젝트
- 뉴스를 **크롤링 → 번역** 후, 본인이 **3개 선택**하면 **카드형 뉴스**로 반환

---

## 3. 🖥️ VS Code, 터미널로 열기

### Step 1 — 작업 폴더로 이동
```powershell
# 탐색기에서 경로 Ctrl+C 후 붙여넣기
cd C:\작업폴더\경로
ls
```

### Step 2 — 가상환경 확인 및 활성화
```powershell
conda env list             # 설치된 가상환경 목록 확인
conda activate (가상환경명) # 원하는 환경 활성화
```

### Step 3 — VS Code 실행
```powershell
code .                     # 현재 폴더를 VS Code로 열기
```

---

## 4. 💡 Miniconda 사용 시
> Anaconda를 사용할 수 없는 환경이라면?

- Miniconda를 그대로 다운로드
- **명령어는 Anaconda와 동일하게** 사용 가능

---

## 5. 🧠 프롬프트 엔지니어링 핵심

### 프롬프트 3요소
| 요소 | 설명 |
|------|------|
| **목적 (Task)** | LLM이 수행해야 할 핵심 과업 |
| **문맥 (Context)** | 과업이 필요한 목적과 배경 정보 |
| **지시사항 (Instructions)** | 결과물의 형식 / 조건 / 세부 지시 |

> Task만으로도 답변 생성은 가능하나, Context + Instructions를 추가하면 **정확성과 만족도가 높아짐**

### Context(맥락/배경)의 역할
- 지시한 과업이 **왜 필요한지**, **어떤 상황인지** 설명
- LLM이 목적과 배경을 이해 → 더 정교하고 실행 가능한 결과 도출
- 단순 아이디어가 아닌, **실제 문제를 반영한 해결책** 가능성 ↑

### 예시 프롬프트
```
현재 노인 인구가 증가하고 은퇴 시기도 늦춰지는 상황이야.
정년 연장, 노인 취업, 복지 예산 등 새로운 정책이 필요한 분위기야.
이와 관련하여 노인 정책 입안을 위한 아이디어를 제시해줘.
```

### 적절한 예시
'''
✓ Few-Shot prompt
✓ 적절한 예시는 상대방의 의도를 더 정확하게 파악할 수 있도록 도움.
✓ 하지만, 예시만 제시하면 인공지능은 어느 패턴(의미 또는 구조)에 초점을
맞출지 모르기 때문에 혼란스러워 함.
✓ Best는 충분한 설명과 함께 예시를 제시할 것.
'''

### Streamlit 소개
- 서버를 돌리는거라고 생각하면 됨
- app.py에 모든 기능을 넣고 돌릴거임
- streamlit app.py 로 구동 (계속돌림)
- 인터넷 주소만 있으면 누구나 접속해서 사용
- 삼성중공업 정책때문에 인터넷에 올리진 못함

### streamlit 순서
- vscode에서 app.py 생성
- 여기에 모든 코드 넣는다
